var config = {
  port: 3000,
  host: 'http://localhost',
  db_options: {
    host: 'localhost',
    port: 3306,
    user: 'root',
    password: '',
    database: 'sync_server'
  }	
};
module.exports = config;